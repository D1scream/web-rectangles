version: "3.9"
services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile.backend
    ports:
      - "8080:8080"

  frontend:
    build:
      context: .
      dockerfile: web-app/Dockerfile.frontend
    ports:
      - "3000:3000"
    environment:
      - API_URL=${API_URL:-}
